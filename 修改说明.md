# 修改说明

## 问题描述

在 headless 模式（如 `grok -p "prompt"`）下运行时，如果当前没有登录且没有可用的认证方式，会报错：

```
Not signed in. Run `grok login` to authenticate (or `grok login --device-code` if no browser is available).
Error: Not signed in. Run `grok login` to authenticate (or `grok login --device-code` if no browser is available).
```

对于无浏览器环境或不想执行 `grok login` 的用户，这个错误会阻止工具使用。

## 修改内容

修改文件：`crates/codegen/xai-grok-pager/src/headless.rs`

1. **优化未登录错误提示**
   - 在交互式和非交互式场景下都优先提示设置 `XAI_API_KEY` 环境变量。
   - 将命令行提示从旧的 `--device-code` 更新为当前主推的 `--device-auth`。

2. **增加 API Key 自动回退**
   - 当 agent 未 advertise 任何可用认证方法，但环境变量 `XAI_API_KEY` 已设置时，直接回退使用 `xai.api_key` 认证方式。
   - 这样用户只需设置 `XAI_API_KEY`，无需运行 `grok login`，即可在 headless 模式下使用工具。

3. **增加 auth.json API Key 自动读取**
   - 在 headless 模式的 `authenticate()` 函数中，如果环境变量 `XAI_API_KEY` 未设置，会主动从 `~/.grok/auth.json` 中读取 `xai::api_key` scope 的 API Key。
   - 读取到 API Key 后自动设置环境变量并继续认证流程。
   - 这与 agent 进程的 `initialize()` 行为保持一致，解决 agent 线程和 pager 线程间的时序依赖问题。

## 使用方式

设置 API Key 后运行：

```bash
export XAI_API_KEY="your-api-key"
grok -p "your prompt"
```

或在 Windows PowerShell：

```powershell
$env:XAI_API_KEY="your-api-key"
grok -p "your prompt"
```

## 测试与构建

- 执行 `cargo check -p xai-grok-pager` / `cargo build -p xai-grok-pager-bin --release` 验证编译通过。
- 在未登录且未设置 `XAI_API_KEY` 时，仍会提示设置 API Key 或运行 `grok login`。
- 在设置 `XAI_API_KEY` 后，headless 模式不再报 “Not signed in” 错误。

## 注意事项

- 该修改仅影响 headless 模式的认证入口；TUI 交互模式保持原有行为。
- 如果管理员通过配置强制禁用 API Key 认证（`disable_api_key_auth`），回退后 agent 仍会在认证阶段拒绝请求。
